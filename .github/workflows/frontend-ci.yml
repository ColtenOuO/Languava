name: Docker Smoke Test

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pass:
    name: Pass placeholder
    runs-on: ubuntu-latest

    steps:
      - name: Pass
        run: echo "Docker smoke test placeholder passed."